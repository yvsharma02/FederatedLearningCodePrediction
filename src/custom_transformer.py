import torch
import torch.nn as nn

class CustomTransformer(nn.Module):

    # Where B is the 
    # Input to Trasnformer will be of Shape (B, W, E)

    # Input to the Transformer will be a matrix of size (B, W)
    # B is the Batch Size.
    # W is the Window Size (Context Length)
    def __init__():
        pass

    def __forward__(self):
        pass